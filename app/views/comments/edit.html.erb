<%= form_for([@project, @comment]) do |f| %>
<div class="container css-2s1366">
  <div class="container profile-container">
    <div class="row">
      <div col-sm-12 col-md-4>
        <%= f.text_area :content, rows: 2, cols: 50, :class => 'form' %>
      </div>
    </div>
    <div class="row">
      <%= f.submit %>
    </div>
    <% end %>
  </div>
</div>
