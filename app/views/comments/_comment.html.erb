<% @comments.each do |comment| %>
  <p><%= comment.content %></p>
  <%= link_to "Edit", edit_project_comment_path(comment.project, comment) %>
  <%= link_to "Delete", [comment.project, comment], method: :delete %>
<% end %>

<%= render 'comments/form'  %>
