<% @page_title = "#{@user.first_name}'s Profile" %>

<div class="container profile-container">
  <h3 class="header"><%= "#{@user.first_name} #{@user.last_name}'s Profile" %></h3>
  <p class="col-sm-11"><%= @user.biography %></p>
  <% if @user == current_user %>
    <div class="col-sm-1"><%= link_to "Edit", edit_user_path(current_user) %></div>
  <% end %>

  <h3 class="header col-sm-12">Projects</h3>
  <p class="col-sm-12">Coming soon..</p>
</div>
